common.py linje 65-78 — pen/button-properties oppretter ny SSH-tilkobling+prosess per aksess. Ikke aktivt et CPU-problem i dag (kun aksessert én gang i evdev.py:99), men samme mønster som Xlib-buggen og bør caches hvis/når --evdev-banen kobles sammen riktig.
evdev.py / remarkable_mouse.py signaturmismatch — read_tablet() kalles med monitor_idx mens evdev.py forventer monitor_num + region, og får en dict i stedet for en reMarkable1/2/Pro-instans. --evdev er dermed trolig helt ødelagt akkurat nå (krasjer umiddelbart).
common.py get_monitor() — kaller get_monitors() unødvendig flere ganger (ikke i hot loop, lav prioritet), og har en UnboundLocalError-bug: hvis monitor_num er ugyldig, forblir monitor ubundet i except-grenen i stedet for å gi en ren feilmelding.